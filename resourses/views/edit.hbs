{{>header}}
{{>nav}}
<section class="my-5 container">
    <form method="post">
        <div class="mb-3">
            <input class="form-control" placeholder="Name" name="name" value="{{user.name}}">
        </div>
        <div class="mb-3">
            <input class="form-control" placeholder="Initial Balance" name="iBalance" value="{{user.iBalance}}">
        </div>
        <div class="mb-3">
            <input class="form-control" placeholder="remaining Balance" name="rBalance" value="{{user.rBalance}}">
        </div>
        <div class="mb-3">
            <button class="btn btn-primary">Edit User</button>
        </div>

    </form>    
</section>
{{>footer}}